name: Release

on:
  push:
    tags: ["v*"]

jobs:
  release:
    uses: diaryx-org/.github/.github/workflows/reusable-plugin-release.yml@main
    with:
      plugin-id: diaryx.templating
      crate-name: diaryx_templating_extism
    secrets:
      REGISTRY_PAT: ${{ secrets.REGISTRY_PAT }}
